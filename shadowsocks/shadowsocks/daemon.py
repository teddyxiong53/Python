from __future__ import absolute_import, print_function, with_statement, division

import os,sys, os.path, logging, signal, time, socket

from shadowsocks import common,shell

def daemon_exec(config):
    if 'daemon' in config:
        pass


